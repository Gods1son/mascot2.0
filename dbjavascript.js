 var db = new SQL.Database();
    // Run a query without reading the results
    db.run("CREATE TABLE faces (ID int identity(1,1) primary key, Name varchar(40), Clues varchar(40);");
    // Insert two rows: (1,111) and (2,222)
    db.run("INSERT INTO faces VALUES (?,?), (?,?)", [phyno,apsahfyngog,lagbaja,lwagfgbafjta]);
